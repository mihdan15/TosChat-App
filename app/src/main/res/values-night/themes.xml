<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WhatsappUI" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/tab_colorDark</item>
        <item name="colorPrimaryVariant">@color/darkmodebg</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/darkmodebg</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/darkmodebg</item>
        <!-- Customize your theme here. -->
        <item name="itemTextColor">@color/text_colorDark</item>
        <item name="colorOnBackground">@color/darkmodegreen</item>
        <item name="textFillColor">@color/white</item>
        <item name="tabBackground">@color/darkmodegreen</item>
        <item name="colorButtonNormal">@color/darkmodegreen</item>
    </style>
</resources>