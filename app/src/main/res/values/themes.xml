<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WhatsappUI" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
<!--        <item name="android:windowFullscreen">true</item>-->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/tab_color</item>
        <item name="colorPrimaryVariant">@color/tab_color</item>
        <item name="colorOnPrimary">@color/tab_color</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/tab_color</item>
        <!-- Customize your theme here. -->
        <item name="itemTextColor">@color/white</item>
        <item name="colorOnBackground">@color/wa</item>
        <item name="textFillColor">@color/black</item>
        <item name="tabBackground">@color/white</item>
        <item name="colorButtonNormal">@color/tab_color</item>

    </style>
</resources>